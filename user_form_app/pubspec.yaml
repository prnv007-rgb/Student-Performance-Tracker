name: user_form_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0' # Updated SDK constraint

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0 # Required for Firebase
  cloud_firestore: ^5.4.4  # Upgrade to the latest version
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
